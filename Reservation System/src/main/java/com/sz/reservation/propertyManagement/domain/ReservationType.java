package com.sz.reservation.propertyManagement.domain;

public enum ReservationType {
    OWNER_APPROVAL, // Reservation is done only if owner approves it
    AUTOMATIC_APPROVAL // Reservation is done automatically without owner approval

}
